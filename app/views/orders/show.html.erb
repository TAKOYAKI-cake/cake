<p>注文履歴詳細</p>
<h4>注文情報</h4>
	<table>
	<tr>
		<th>注文日</th>
		<td><%= @order.created_at.strftime('%Y/%m/%d') %></td>
	</tr>
	<tr>
		<th>配送先</th>
		<td>
	      <%= @order.postcode %><br>
	      <%= @order.shipping_address %><br>
	      <%= @order.shipping_name %>
	    </td>
	</tr>
	<tr>
		<th>支払い方法</th>
		<td><%= @order.method_of_payment %></td>
	</tr>
	<tr>
		<th>ステータス</th>
		<td><%= @order.order_status %></td>
	</tr>
</table>

<h4>請求情報</h4>
<table>
	<tr>
	<th>配送料</th>
	<td><%= @order_postage %></td>
	</tr>
	<% @sum = 0 %>
	<% @orderd_products.each do |o_product| %>
	<tr>
        <% @sum += o_product.product.price * o_product.quantity %>
		<% end %><br>
        <th>商品合計</th>
		<td><%= @sum %></td>
	</tr>
	<tr>
		<th>ご請求金額</th>
		<td><%= @order.total_amount %></td>
	</tr>
</table>

<h4>注文内容</h4>
<table>
		<% @orderd_products.each do |o_product| %>
	<tr>
		<th>商品</th>
		<th>単価(税込)</th>
		<th>個数</th>
		<th>小計</th>
	</tr>
	<tr>
		<td><%= o_product.product.name %></td>
		<td><%= o_product.product.price %></td>
		<td><%= o_product.quantity%></td>
		<td><%= o_product.product.price * o_product.quantity %></td>
	<% end %>
	</tr>
</table>
